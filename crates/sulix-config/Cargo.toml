[package]
name = "sulix-config"
version = "0.1.0"
edition = "2021"
description = "Sulix Intelligence — 配置加载与类型"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
anyhow = "1"
log = "0.4"
